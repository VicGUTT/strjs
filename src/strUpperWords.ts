import strUcwords from './strUcwords.js';

/**
 * Uppercase the first character of each word in a string.
 *
 * @alias strUcwords This function is an alias of the `strUcwords` function.
 */
const strUpperWords = strUcwords;

export default strUpperWords;
