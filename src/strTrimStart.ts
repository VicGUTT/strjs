import strLtrim from './strLtrim';

/**
 * Trim the left side (start) of a string of the given characters.
 *
 * @alias strLtrim This function is an alias of the `strLtrim` function.
 */
const strTrimStart = strLtrim;

export default strTrimStart;
